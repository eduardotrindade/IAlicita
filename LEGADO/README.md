# LEGADO

Arquivos removidos do código-fonte mas mantidos para referência histórica.

## Arquivos

- `mockData.ts` — Funções `fetchProcurements`, `SCORE_BANDS`, `procurementMatchesScore` (não utilizadas). A função `countByUf` foi movida para `src/utils/countByUf.ts`.
